## Decisions

During the Dog discussion we discovered that behaviour cannot be described without context.

Instead of continuing the Dog model, we introduced Context as a core Product Model concept.

Dog was intentionally postponed.

Reason:

The Product Model should describe behaviour in context rather than globally.